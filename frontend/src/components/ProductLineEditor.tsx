export function ProductLineEditor() {
  return (
    <section className="panel">
      <p className="eyebrow">Phase 3 target</p>
      <h2>Product Line Review</h2>
      <p>
        Suggested line generation, editing, and silent interaction mode are not
        wired yet. This panel keeps the workflow visible in the dashboard shell.
      </p>
    </section>
  );
}
