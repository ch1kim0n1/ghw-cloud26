export function CampaignForm() {
  return (
    <section className="panel">
      <p className="eyebrow">Phase 1 target</p>
      <h2>Campaign Intake</h2>
      <p>
        Campaign creation and source-video upload are intentionally stubbed in
        Phase 0. The surrounding layout is live so the next phase can wire
        actual form behavior without changing the page structure.
      </p>
    </section>
  );
}
