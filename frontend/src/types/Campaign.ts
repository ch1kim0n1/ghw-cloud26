export interface Campaign {
  campaign_id: string;
  job_id: string;
  product_id: string;
  name: string;
  video_filename: string;
  video_path: string;
  target_ad_duration_seconds: number;
  created_at: string;
}
